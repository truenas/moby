package middleware

var (
	middlewareClientPath         = "midclt"
	configDir                    = "/etc/docker"
	configFile                   = "middleware.json"
	clientConfig         *config = nil
)
